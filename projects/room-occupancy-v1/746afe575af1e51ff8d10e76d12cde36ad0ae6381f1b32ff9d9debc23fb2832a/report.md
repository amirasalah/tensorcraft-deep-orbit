# Infrastructure verification only

The maintainer fixture trains a small deterministic model to verify export, persistence and external inference. No human rubric score or course mastery is claimed.

The training settings and split are in train.mjs and plan.json. All metrics are reproducible local fixture measurements.
